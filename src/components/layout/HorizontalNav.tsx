import React from 'react';
import { 
  LayoutGrid, 
  PanelsTopLeft,
  FileText
} from 'luc

  id: stri
  icon: Luc
};
const NAV_ITEMS: NavIt
  { id: 'custom-dashboard', label: 'Dashboard C

  { id: 'mainten
  { id: 'sett

  currentPage: stri
}
fu

export const HorizontalNav: Re
    <nav 
      aria-label="Seções"
    >
        const Icon = item.icon;
        
          <button
            onClick={() => onNavigate(item.id)}
            className={cn(
  

            )}
            <Icon clas
          </button>
 

































      })}

  );
